{
  "name": "qulabinfinite-dev",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" }
  },
  "postCreateCommand": "python -m pip install -U pip && if [ -f requirements.txt ]; then pip install -r requirements.txt; elif [ -f pyproject.toml ]; then pip install -e .; fi && if [ -f package.json ]; then npm ci; fi",
  "forwardPorts": [8000, 5173],
  "portsAttributes": {
    "8000": { "label": "QuLab API" },
    "5173": { "label": "QuLab Web" }
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python","ms-python.vscode-pylance","ms-azuretools.vscode-docker"]
    }
  }
}
